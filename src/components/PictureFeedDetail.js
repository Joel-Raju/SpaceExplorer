import React from 'react';
import { ScrollView, Text } from 'react-native';

const PictureFeedDetail = (props) => {
  return (
    <ScrollView>
      <Text>Hello World</Text>
    </ScrollView>
  );
};

export default PictureFeedDetail;
